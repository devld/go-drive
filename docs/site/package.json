{
  "name": "go-drive-docs",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "npm run check:translations && vitepress build && npm run check:seo",
    "preview": "vitepress preview",
    "check:translations": "node scripts/check-translations.mjs",
    "check:seo": "node scripts/check-seo.mjs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
